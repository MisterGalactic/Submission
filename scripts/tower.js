//Describe tower features
const towerTypeOne = {
  $elem: null,
  id: `_${Math.random().toString(36).substring(2, 15)}`, //scrutiny needed
  dimension: initDimension,
  position: initPos,
  background: initBackground,
  shooting range: TOWER_RADIUS,
  damage: TOWER_DP
  }

const towerTypeTwo = {
  $elem: null,
  id: `_${Math.random().toString(36).substring(2, 15)}`, //scrutiny needed
  dimension: initDimension,
  position: initPos,
  background: initBackground,
  shooting range: TOWER_RADIUS,
  damage: TOWER_DP
  }

const towerTypeThree = {
  $elem: null,
  id: `_${Math.random().toString(36).substring(2, 15)}`, //scrutiny needed
  dimension: initDimension,
  position: initPos,
  background: initBackground,
  shooting range: TOWER_RADIUS,
  damage: TOWER_DP
  }


// map grid alongside pathway
// const towerRoadGrid = [
//   r = [null,null,null,null,null,null,null,null],  // a 8x2 grid
//   c = [null,null,null,null,null,null,null,null]
// ]
// Let newRow = r, newCol = c

//finish